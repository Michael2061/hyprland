{
    "layer": "top",
    "position": "top",
    "height": 60, // Bleib bei 60, das gibt den Icons Platz zum Atmen
    "margin-top": 10,
    "margin-left": 15,
    "margin-right": 40, // Erhöht von 15 auf 25, damit es weiter rechts "reingeht"
    "spacing": 12,

    "modules-left": ["custom/launch", "hyprland/workspaces"],
    "modules-center": ["custom/music"],
    "modules-right": [
        "cpu#per-core",
        "memory",
        "temperature",
        "pulseaudio",
        "network",
        "clock",
        "tray",
        "custom/power"
    ],

    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate",
        "all-outputs": true,
        "sort-by-number": true
    },

    "network": {
        "format-wifi": "  {essid}  {bandwidthUpBytes}  {bandwidthDownBytes}",
        "format-ethernet": "󰈀   {bandwidthUpBytes}  {bandwidthDownBytes}",
        "format-linked": "󰈀 {ifname} (No IP)",
        "format-disconnected": "󰖪  Disconnected",
        "interval": 2,
        "tooltip-format": "{ifname} via {gwaddr} 󰊗",
        "max-length": 50
    },

    "custom/music": {
        "format": "󰝚  {}",
        "escape": true,
        "interval": 2,
        "exec": "playerctl metadata --format '{{ title }}' 2>/dev/null || echo 'Silent'",
        "on-click": "playerctl play-pause",
        "max-length": 50
    },

    "memory": {
        "format": "  {used:0.1f}G/{total:0.1f}G",
        "tooltip-format": "{percentage}% genutzt",
        "interval": 5
    },

    "cpu#per-core": {
        "format": "  {usage}%",
        "on-click": "kitty --class float_kitty btop",
        "interval": 2
    },

    "temperature": {
        "critical-threshold": 80,
        "format": "{icon} {temperatureC}°C",
        "format-icons": ["", "", "", "", ""]
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "󰝟",
        "format-icons": {
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },

    "clock": {
        "format": "󰃰  {:%H:%M}",
        "format-alt": "󰃭  {:%d.%m.%Y}",
        "tooltip-format": "<tt><small>{calendar}</small></tt>"
    },

    "custom/launch": {
        "format": "󰈸",
        "on-click": "rofi -show drun",
        "tooltip": false
    },

    "custom/power": {
        "format": "⏻",
        "on-click": "wlogout",
        "tooltip": false
    },

    "tray": {
        "icon-size": 18,
        "spacing": 10
    }
}
