{
    "layer": "top",
    "position": "top",
    "height": 45, // Kompakt aber genug Platz
    "margin-left": 10,
    "margin-right": 10,
    "margin-top": 5,

    "modules-left": [
        "custom/launcher",
        "hyprland/workspaces"
    ],

    "modules-center": [
        "custom/music",
        "clock"
    ],

    "modules-right": [
        "cpu",
        "memory",
        "network",
        "pulseaudio",
        "tray",
        "custom/power"
    ],

    "custom/launcher": {
        "format": "   ", // Das CachyOS/Linux Symbol
        "on-click": "rofi -show drun",
        "tooltip": false
    },

    "custom/music": {
        "format": "󰝚  {} ",
        "exec": "playerctl metadata --format '{{artist}} - {{title}}' 2>/dev/null || echo 'Musik aus'",
        "interval": 1,
        "max-length": 30
    },

    "clock": {
        "interval": 60,
        "format": "   {:%H:%M  󰃭  %d.%m.%Y} ", // %Y fügt das Jahr 2026 hinzu
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "cpu": {
        "interval": 2,
        "format": "   {usage}% ",
        "tooltip": true // Im Tooltip stehen die Cores meistens trotzdem automatisch drin
    },

    "memory": {
        "interval": 2,
        "format": "   {percentage}% ({total:0.1f}GiB) "
    },

    "network": {
        "interval": 2,
        "format-wifi": "   {essid}  {bandwidthUpBytes}  {bandwidthDownBytes} ",
        "format-ethernet": " 󰈀   {bandwidthUpBytes}  {bandwidthDownBytes} ",
        "format-disconnected": " 󰖪  Offline "
    },

    "pulseaudio": {
        "format": " {icon} {volume}% ",
        "format-muted": " 󰝟 ",
        "format-icons": { "default": ["󰕿", "󰖀", "󰕾"] }
    },

    "custom/power": {
        "format": " 󰐦  ", // Das Icon mit Leerzeichen wie von dir entdeckt!
        "on-click": "wlogout",
        "tooltip": false
    }
}
