{
    "layer": "top",
    "position": "top",
    "height": 40,
    "margin-left": 15,
    "margin-right": 15,
    "margin-top": 10,
    "spacing": 5,

    "modules-left": ["custom/launcher", "hyprland/workspaces"],
    "modules-center": ["custom/music", "clock"],
    "modules-right": ["cpu", "memory", "network", "pulseaudio", "tray", "custom/power"],

    "custom/launcher": {
        "format": "   ",
        "on-click": "rofi -show drun -theme ~/.config/rofi/rosie.rasi",
        "tooltip": false
    },

    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate",
        "all-outputs": false,
        "sort-by-number": true
    },

    "custom/music": {
        "format": "󰝚  {} ",
        "exec": "playerctl metadata --format '{{artist}} - {{title}}' 2>/dev/null || echo 'Musik aus'",
        "interval": 1,
        "on-click": "playerctl play-pause",
        "max-length": 35
    },

    "clock": {
        "format": "  {:%H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "cpu": {
        "interval": 2,
        "format": "  {usage}%",
        "tooltip": true
    },

    "memory": {
        "interval": 2,
        "format": "  {percentage}%"
    },

    "network": {
        "format-wifi": "  {essid}",
        "format-ethernet": "󰈀  {ifname}",
        "format-disconnected": "󰖪  Offline",
        "interval": 5
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "󰝟 ",
        "format-icons": { "default": ["󰕿", "󰖀", "󰕾"] }
    },

    "custom/power": {
        "format": " ",
        "on-click": "wlogout",
        "tooltip": false
    }
}
