{
    "layer": "top",
    "position": "top",
    "height": 50,
    "margin-top": 8,
    "margin-left": 10,
    "margin-right": 10,
    "spacing": 10,

    "modules-left": ["custom/launch", "hyprland/workspaces", "network"],
    "modules-center": ["custom/music"],
    "modules-right": ["cpu", "memory", "pulseaudio", "tray", "clock"],

    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate",
        "all-outputs": true
    },

    "network": {
        "format-wifi": "  {bandwidthDownBits}",
        "format-ethernet": "󰈀  {bandwidthDownBits}",
        "interval": 2
    },

    "custom/music": {
        "format": "󰝚  {}",
        "escape": true,
        "interval": 5,
        "exec": "playerctl metadata --format '{{ title }}' 2>/dev/null || echo 'Kein Player aktiv'",
        "on-click": "playerctl play-pause",
        "max-length": 50
    },

    "cpu": {
        "format": " {usage}%"
    },

    "memory": {
        "format": "󰍛 {used}GB"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "󰝟",
        "format-icons": { "default": ["", "", ""] }
    },

    "clock": {
        "format": "{:%H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "custom/launch": {
        "format": "󰈸",
        "on-click": "rofi -show drun"
    },

    "tray": {
        "spacing": 10
    }
}
